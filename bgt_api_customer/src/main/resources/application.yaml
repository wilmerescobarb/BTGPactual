spring:
  application:
    name: bgt_api_customer

  data:
    mongodb:
      host: localhost
      port: 27017
      database: bgt_db

server:
  port: 8084

jwt:
  # Debe coincidir exactamente con el secret de bgt_api_auth
  secret: bgt-super-secret-key-for-jwt-signing-2026

logging:
  level:
    com.ceiba.bgt_api_customer: INFO
